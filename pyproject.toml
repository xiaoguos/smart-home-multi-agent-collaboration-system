[project]
name = "a2a-hardware-serve"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.8",
    "httpx>=0.28.1",
    "langchain-google-genai>=2.0.10",
    "langgraph>=0.3.18",
    "langchain-openai>=0.1.0",
    "pydantic>=2.10.6",
    "python-dotenv>=1.1.0",
    "uvicorn>=0.34.2",
    "sse-starlette>=2.3.6",
    "starlette>=0.46.2",
    "a2a-sdk[all]>=0.3.0",
    "pycryptodome>=3.19.0",
    "requests>=2.31.0",
    "Pillow>=10.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["agent_collter"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
